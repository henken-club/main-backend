type Henken implements Node {
  id: ID!
}

interface Node {
  id: ID!
}

type FindHenkenPayload {
  henken: Henken
}

type Query {
  henken(id: ID!): Henken!
  findHenken(id: ID!): FindHenkenPayload!
}
